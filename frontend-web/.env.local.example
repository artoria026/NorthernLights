VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_APP_ENV=development

# Shows the "Usar cuenta de prueba" button on /login -- also requires dev
# mode (import.meta.env.DEV, false in any `vite build`). Leave empty or
# false to not see it even when running `vite dev`.
VITE_ENABLE_DEV_LOGIN=true
